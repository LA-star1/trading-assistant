# AI交易助手 后端依赖

# Web 框架
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# HTTP 客户端（AI API 调用）
httpx>=0.27.0

# 数据获取
akshare>=1.12.0

# 数据处理
pandas>=2.0.0
numpy>=1.26.0
openpyxl>=3.1.0        # Excel 读写

# 加密（券商密码加密存储）
cryptography>=42.0.0

# 可选：东财Web同步相关
# requests>=2.31.0
# pycryptodome>=3.20.0   # RSA 加密密码
# ddddocr>=1.4.11        # 验证码识别（需要额外安装）

# 可选：QMT 同步
# xtquant                # 通过 QMT 客户端安装包安装，不在 PyPI

# 开发/测试
# pytest>=8.0.0
# httpx>=0.27.0           # fastapi TestClient 依赖（已包含）
